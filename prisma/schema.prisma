
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

// Tworzę model user do bazy postgreSQL
model User {
  id    Int    @id @default(autoincrement())
  name  String
  email String
}
